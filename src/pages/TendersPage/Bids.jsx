import React, {useState} from 'react';

const Bids = () => {
    const [bids, setBids] = useState([])

    return (
        <div>
            there will be bids
        </div>
    );
};

export default Bids;